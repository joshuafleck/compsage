<li>	
	<h3><%=survey.job_title%>
		<span class="sponsor">sponsored by <%=link_to survey.sponsor.name, organization_path(survey.sponsor)%></span></h3>
	<div class="description_box">
		<%=survey.description%> <%=link_to "More&nbsp;details…", survey_path(survey)%></a>
	</div>
	<div class="actions"><%=link_to "Participate Now", survey_path(survey)%> (<%=distance_of_time_in_words_to_now(survey.end_date)%> left)</div>
</li>