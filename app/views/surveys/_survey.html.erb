<li>	
  <h3>
    <%= link_to survey.job_title, survey_path(survey) %>
    <span class="sponsor">sponsored by <%= link_to survey.sponsor.name, organization_path(survey.sponsor) %></span>
  </h3>
  
  <div class="description_box">
		<%= link_and_format(survey.description.gsub(/\b([A-Z][A-Z0-9]{2,})\b/, '<span class="abbrev">\1</span>')) %></a>
	</div>
  <div class="deadline">Participation deadline in <%= better_time_ago_in_words(survey.end_date) %></div>
</li>
