<li>	
  <h3>
    <%= link_to survey.job_title, survey_path(survey) %>
    <span class="sponsor">sponsored by <%= link_to survey.sponsor.name, organization_path(survey.sponsor) %></span>
  </h3>
  
  <div class="description_box">
		<%= auto_link(survey.description.gsub(/\b([A-Z][A-Z0-9]{2,})\b/, '<span class="abbrev">\1</span>'), :href_options => { :target => '_blank' }) %></a>
	</div>
  <div class="deadline">Participation deadline in <%= distance_of_time_in_words_to_now(survey.end_date) %></div>
</li>
