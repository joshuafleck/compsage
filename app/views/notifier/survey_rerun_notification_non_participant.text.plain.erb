The response deadline has been extended for the survey "<%=@survey.job_title%>"

If you would like to participate in this survey, go to: <%= @recipient.is_a?(ExternalSurveyInvitation) ? survey_login_path(:survey_id => @survey.id, :key => @recipient.key, :only_path => false) : url_for(survey_path(@survey, :only_path => false))%>

Survey: <%=@survey.job_title%>
Description: <%=@survey.description%>
Sponsor: <%=@survey.sponsor.name%>
Response deadline: <%=time_ago_in_words(@survey.end_date)%>

Thanks,
CompSage.com
