Results for the survey "<%=@survey.job_title%>" are now available!

To view the survey results, go to: <%= @participant.is_a?(ExternalSurveyInvitation) ? survey_login_path(:survey_id => @survey.id, :key => @participant.key, :only_path => false, :subdomain => subdomain_for(:email => @participant.email)) : url_for(survey_report_path(@survey, :only_path => false, :subdomain => subdomain_for(:organization => @participant)))%>

Thanks,
CompSage.com
