require File.dirname(__FILE__) + '/../../spec_helper'

describe "/messages/index" do

  before(:each) do
    render 'messages/index'
  end

  it "should show the list of messages" do
  	pending
  end
  
  it "should allow pagination..." do
  	pending
  end
  
  it "should have a means for sorting the messages..." do
  	pending
  end
  
  it "should render the message subjects with a link to show the message" do
  	pending
  end
  
  it "should have a link for deleting each message" do
  	pending
  end
  
  it "should have a link for replying to each message" do
  	pending
  end
  
  it "should display the date/time the message was received" do
  	pending
  end
  
  it "should display the sender with a link to the sender's page" do
  	pending
  end
  
  it "should denote unread messages" do
  	pending
  end

  it "should have a link for creating a new message" do
 		pending
 	end

  it "should have a link to the sent messages page" do
  	pending
  end
  
end
