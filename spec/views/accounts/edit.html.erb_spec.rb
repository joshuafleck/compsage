require File.dirname(__FILE__) + '/../../spec_helper'

describe "accounts/edit" do

  it "should render the edit form"
  it "should prepopulate all editable fields with their current values"
  it "should have a means for allowing the org to change its email address"
  it "should have a means for allowing the org to change its password"	
  it "should have a means for allowing the org to change public/private status"  
  it "should have a means for allowing the org to change its location"
  it "should have a means for allowing the org to change its city"
  it "should have a means for allowing the org to change its state"
  it "should have a means for allowing the org to change its contact name"
  it "should have a submit button"
  it "should have a cancel button that links back to the show page"
  it "should have a link for explaining private mode"
end
